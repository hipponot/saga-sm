import { describe, it, expect } from 'vitest';

describe('API Types Package', () => {
  it('should export types correctly', () => {
    // Simple test to ensure the package loads
    expect(true).toBe(true);
  });
});
