{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm db:generate && tsup",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --name auto-generated-migration --skip-generate",
    "db:deploy": "prisma migrate deploy",
    "db:push": "prisma db push --accept-data-loss",
    "test": "echo 'No database-specific tests defined'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "prisma": "^6.16.0",
    "tsup": "^8.0.2",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@prisma/client": "^6.16.0"
  },
  "exports": {
    ".": "./dist/index.js"
  }
}