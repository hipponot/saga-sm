{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --name auto-generated-migration --skip-generate",
    "db:deploy": "prisma migrate deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "prisma": "^6.16.0",
    "tsup": "^8.0.2",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@prisma/client": "^6.16.0"
  },
  "exports": {
    ".": "./dist/index.js"
  }
}